# Local Development Configuration
# Uses localhost PostgreSQL
# Override via environment variables: DB_HOST, DB_PORT, DB_NAME, DB_USERNAME, DB_PASSWORD

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:tiffany_cambodia}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:Hour1819}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    root: INFO
    com.tiffany: INFO
    # Spring framework
    org.springframework: WARN
    org.springframework.web: WARN
    org.springframework.security: WARN
    # Hibernate / JPA
    org.hibernate: WARN
    org.hibernate.SQL: WARN
    org.hibernate.type.descriptor.sql: WARN
    # Tomcat / Catalina
    org.apache.catalina: WARN
    org.apache.tomcat: WARN
    org.apache.coyote: WARN
    # Cache / Redis
    com.github.benmanes.caffeine: WARN
    io.lettuce.core: WARN
    # Connection pool
    com.zaxxer.hikari: WARN
